services:
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: restful-api-proxy-backend
    working_dir: /app
    command:
      [
        "uv",
        "run",
        "uvicorn",
        "server:server",
        "--host",
        "0.0.0.0",
        "--port",
        "8000",
        "--reload"
      ]
    ports:
      - "8000:8000"
    environment:
      OLLAMA_API: http://ollama:11434/api/chat
      RATE_LIMIT_STORAGE_URI: redis://redis:6379
    depends_on:
      redis:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/docs"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - restful-api-proxy-net

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
    container_name: restful-api-proxy-frontend
    working_dir: /app
    command:
      [
        "npm",
        "run",
        "dev"
      ]
    ports:
      - "4321:4321"
    environment:
      PUBLIC_API_BASE_URL: http://backend:8000
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - restful-api-proxy-net

  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    environment:
    - NVIDIA_VISIBLE_DEVICES=all
    - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    networks:
      - restful-api-proxy-net

  redis:
    image: redis:alpine
    container_name: redis_db
    command: redis-server --appendonly yes
    ports:
      - 6379:6379
    volumes:
      - redis_data:/data
    restart: always
    networks:
      - restful-api-proxy-net

volumes:
  ollama_data:
    name: ollama_data
  redis_data:
    name: redis_data

networks:
  restful-api-proxy-net:
    driver: bridge
